using System;
using System.Collections.Generic;
using FullSerializer;

// Token: 0x020002B5 RID: 693
[fsObject(MemberSerialization = fsMemberSerialization.OptOut)]
public partial class TeamManager : GenericManager<Team>
{
	// Token: 0x06004CE4 RID: 19684
	public void TeamBuildingDecay()
	{
		int numberofteams = Game.instance.teamManager.count;
		for (int i = 0; i < numberofteams; i++)
		{
			if (0.02 < (double)RandomUtility.GetRandom01() && Game.instance.teamManager.GetEntity(i).headquarters.GetBuilding(HQsBuildingInfo.Type.TestTrack).currentLevel > 0)
			{
				Game.instance.teamManager.GetEntity(i).headquarters.GetBuilding(HQsBuildingInfo.Type.TestTrack).BuildingDecay();
			}
			if (0.02 < (double)RandomUtility.GetRandom01() && Game.instance.teamManager.GetEntity(i).headquarters.GetBuilding(HQsBuildingInfo.Type.TelemetryCentre).currentLevel > 0)
			{
				Game.instance.teamManager.GetEntity(i).headquarters.GetBuilding(HQsBuildingInfo.Type.TelemetryCentre).BuildingDecay();
			}
			if (0.02 < (double)RandomUtility.GetRandom01() && Game.instance.teamManager.GetEntity(i).headquarters.GetBuilding(HQsBuildingInfo.Type.BrakesResearchFacility).currentLevel > 0)
			{
				Game.instance.teamManager.GetEntity(i).headquarters.GetBuilding(HQsBuildingInfo.Type.BrakesResearchFacility).BuildingDecay();
			}
			if (0.02 < (double)RandomUtility.GetRandom01() && Game.instance.teamManager.GetEntity(i).headquarters.GetBuilding(HQsBuildingInfo.Type.WindTunnel).currentLevel > 0)
			{
				Game.instance.teamManager.GetEntity(i).headquarters.GetBuilding(HQsBuildingInfo.Type.WindTunnel).BuildingDecay();
			}
			if (0.02 < (double)RandomUtility.GetRandom01() && Game.instance.teamManager.GetEntity(i).headquarters.GetBuilding(HQsBuildingInfo.Type.Simulator).currentLevel > 0)
			{
				Game.instance.teamManager.GetEntity(i).headquarters.GetBuilding(HQsBuildingInfo.Type.Simulator).BuildingDecay();
			}
			if (0.02 < (double)RandomUtility.GetRandom01() && Game.instance.teamManager.GetEntity(i).headquarters.GetBuilding(HQsBuildingInfo.Type.RideHandlingDevelopment).currentLevel > 0)
			{
				Game.instance.teamManager.GetEntity(i).headquarters.GetBuilding(HQsBuildingInfo.Type.RideHandlingDevelopment).BuildingDecay();
			}
			if (0.02 < (double)RandomUtility.GetRandom01() && Game.instance.teamManager.GetEntity(i).headquarters.GetBuilding(HQsBuildingInfo.Type.Factory).currentLevel > 0)
			{
				Game.instance.teamManager.GetEntity(i).headquarters.GetBuilding(HQsBuildingInfo.Type.Factory).BuildingDecay();
			}
			if (0.02 < (double)RandomUtility.GetRandom01() && Game.instance.teamManager.GetEntity(i).headquarters.GetBuilding(HQsBuildingInfo.Type.DesignCentre).currentLevel > 0)
			{
				Game.instance.teamManager.GetEntity(i).headquarters.GetBuilding(HQsBuildingInfo.Type.DesignCentre).BuildingDecay();
			}
		}
	}
}
